# Swarm 研究产物契约

最终中文摘要必须包含：

1. 研究对象、objective、as-of、run id、preset。
2. 实际 provider、model；provider 应为 `openai-codex`，配置模型应为 `openai-codex/gpt-5.6-sol`，实际请求模型应为 `gpt-5.6-sol`，reasoning effort 应为 `medium`。禁止使用裸名称 `5.6-sol`。
3. 可核验事实与来源日期。
4. 最强支持情景和最强反对情景。
5. 被接受、被拒绝和仍未解决的 claims。
6. 假设审计、证伪所需证据、数据新鲜度和来源质量。
7. confidence、limitations 和后续研究清单。
8. `RESEARCH_ONLY`、`WATCH` 或 `NO_TRADE` 研究质量标签。
9. 明确边界：研究证据不能生成或升级 Trade Plan Card、execution status 或 broker action。

不得输出仓位比例、确定性方向、入场单、止损单、止盈单或任何券商写操作。
