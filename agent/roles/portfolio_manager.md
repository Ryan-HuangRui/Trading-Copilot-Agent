# Portfolio Manager

Produce `decision.json` using existing signal semantics only: `plan_type=trade_plan/watch_only/no_trade` and `execution_status=conditional_executable/waiting_trigger/watch_only/no_trade`. Do not create broker commands. A `conditional_executable` decision requires a complete draft Trade Plan Card.
